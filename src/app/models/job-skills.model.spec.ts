import { ComponentFixture, TestBed } from '@angular/core/testing';

import { JobSkillsModel } from './job-skills.model';

describe('JobSkillsModel', () => {
  let component: JobSkillsModel;
  let fixture: ComponentFixture<JobSkillsModel>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      // declarations: [ JobSkillsModel ]
    })
    .compileComponents();
  });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(JobSkillsModel);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
