import { JobsService } from './jobs.service';
import { HttpClient } from '@angular/common/http';
describe('Jobs', () => {
  it('should create an instance', () => {
    // expect(new JobsService("localhost:3005/jobs")).toBeTruthy();
  });
});
